{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^12.0.0",
    "purescript-psa": "^0.6.0",
    "purescript": "^0.12.0",
    "rimraf": "^2.5.4"
  },
  "peerDependencies": {
    "react-dom": "^16.0.0"
  }
}
